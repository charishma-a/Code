mutation PrepareCreditCardDeposit($input: PrepareCreditCardDepositInput!) {
  prepareCreditCardDeposit(input: $input) {
    clientSecret
    depositId
    deposit {
      ...BasicDeposit
    }
    status
  }
}
